# Pointer registry only (name + GitHub URL). This file is NOT turned into
# TeamCity subprojects. Each app repo has its own .teamcity and TeamCity project.
# Written by factory new-api (scripts/service-git.sh).
